#!/bin/bash -e

# Run database migrations if connected
if [ "${SKIP_MIGRATIONS}" != "true" ]; then
  echo "Running database migrations..."
  bundle exec rails db:migrate 2>/dev/null || echo "Migration skipped (DB not available)"
fi

exec "${@}"
